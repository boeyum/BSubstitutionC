<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jib.no.app.bsubstitutionc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YeastTypesActivity"></activity>
        <activity android:name=".YeastCardActivity" />
        <activity android:name=".YeastSubstituteActivity" />
        <activity android:name=".BCInfoActivity" />
        <activity android:name=".HopsReplacementActivity" />
        <activity android:name=".HopsSubstituteActivity" />
        <activity android:name=".GrainTypeActivity" />
        <activity android:name=".GranReplacementActivity" />
        <activity android:name=".GrainSubstituteActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>